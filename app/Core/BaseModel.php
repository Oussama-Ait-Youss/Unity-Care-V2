<?php

abstract class BaseModel {
    // This allows children to extend it without error.
    // We can add common logic here later.
    
    public function __construct() {
        // Empty for now
    }
}